# Data directory

`example/` contains a newly generated demonstration instance and scenarios. It exists only to document the file schema.

Do not label the example as the original experimental dataset. The exact data supporting the manuscript tables should be deposited separately with immutable checksums and, preferably, a Zenodo DOI.
